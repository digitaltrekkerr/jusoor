/// A parser for plain text content that returns the input string as-is.
class PlainTextParser {
  /// Returns [content] without any transformation.
  String parse(String content) {
    return content;
  }
}
