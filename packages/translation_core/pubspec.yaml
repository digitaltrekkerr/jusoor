name: translation_core
version: 0.1.0
resolution: workspace

environment:
  sdk: "^3.10.8"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0